using UnityEngine;

public class AudioContinuo : MonoBehaviour
{
    private static AudioContinuo instance;
    void Awake()
    {
        if (instance == null)
        {
            instance = this;
            DontDestroyOnLoad(gameObject);
        }
        else
        {
            Destroy(gameObject);
        }
    }
}